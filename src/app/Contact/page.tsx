// app/contact/page.tsx
import React from 'react';

// The function name (ContactPage) doesn't matter, but it MUST be a default export.
export default function ContactPage() {
  return (
    <div>
      <h1>Contact Us</h1>
      <p>This is the content for the /contact route.</p>
    </div>
  );
}